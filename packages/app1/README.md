# app1

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build app1` to build the library.

## Running unit tests

Run `nx test app1` to execute the unit tests via [Jest](https://jestjs.io).
